docker run --name keycloak_test -p 8543:8080 \
        -e KEYCLOAK_ADMIN=admin -e KEYCLOAK_ADMIN_PASSWORD=password \
        quay.io/keycloak/keycloak:23.0.7 \
        start-dev

After running above command you can import realm-quarks-export-keycloak.json